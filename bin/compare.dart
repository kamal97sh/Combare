import 'dart:io';
void main(List<String> arguments) {

     String fristName=stdin.readLineSync()!;
     String secondName=stdin.readLineSync()!;
 
       if(fristName==secondName)
       {
         print("Same Words");
       }
       else
        {
          ("not Same Words");
        }
}
